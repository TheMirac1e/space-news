import { Typography } from "@mui/material"

export const TotalCount = () => {
  return (
    <Typography
      component="div"
      className="flex items-center justify-between text-black text-base border-0 border-b border-solid border-gray-300 mb-4"
    >
      <div>total</div>
    </Typography>
  )
}
